{
  "name": "ext-react",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "./build.sh",
    "zip": "rm build.zip && zip -r build.zip build/"
  },
  "devDependencies": {
    "web-ext": "^6.6.0"
  }
}

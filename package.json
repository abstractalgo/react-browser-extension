{
  "name": "ext-react",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:setup": "rm -rf dist && mkdir dist",
    "build:meta": "cp manifest.json dist/manifest.json && cp icon.png dist/icon.png",
    "build:options": "cp -v options/* dist",
    "build:popup": "cp popup/popup.html dist/popup.html",
    "build:worker": "tsc worker/background.ts --outDir dist",
    "build:content": "cp content_scripts/content-root.css dist/content-root.css && tsc content_scripts/content-root.ts --outDir dist && (tsc --removeComments content_scripts/app/config-overrides.ts || true) && cd content_scripts/app && (yarn react-app-rewired build || true) && cp build/main.js ../../dist/content-react.js",
    "build": "yarn build:setup && yarn build:meta && yarn build:options && yarn build:popup && yarn build:worker && yarn build:content"
  }

}